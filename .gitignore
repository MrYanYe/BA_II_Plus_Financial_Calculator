# Generated by tools/extract_calculator.py. Fully reproducible from
# upstream_raw/, so it is not versioned -- src/ holds the authored files, build/
# holds the derived ones.
build/

# Verification screenshots -- regenerated by tools/verify_visual.py on every run,
# useful to look at locally but not worth versioning (they churn on font-hinting
# changes and are ~64KB each).
.verify_shots/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
